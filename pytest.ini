[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# Tests are offline and never touch data/collection/*.csv or data/decks/* — everything
# they read or write lives in pytest's tmp_path.
