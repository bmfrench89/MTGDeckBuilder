# Verify queue — one card name per line. Pushed on a claude/** branch, this file
# triggers .github/workflows/deck-verify.yml (verbatim Scryfall text in the job log).
#
# Queued 2026-08-20: The Ur-Dragon itself. The player asked whether the tuning work
# accounted for EMINENCE, and the answer is that nothing in this repo models it —
# `grep -rin "eminence" scripts/ data/reference/` returns NOTHING. Before quantifying
# what that omission costs, the commander's own text has to be on disk rather than
# recalled: it is the single most load-bearing card in the deck and every cost
# assumption in the goldfish analysis depends on reading it exactly.
The Ur-Dragon
