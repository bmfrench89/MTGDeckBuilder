# Verify queue — one card name per line. Pushed on a claude/** branch, this file
# triggers .github/workflows/deck-verify.yml (verbatim Scryfall text in the job log).
#
# Queued 2026-08-20: Mana Drain was added to yshtola-nights-blessed at the player's
# direction. Its text is common knowledge ({U}{U} counter; bank the countered
# spell's mana value as colorless next main phase) but the deck notes cite it, so
# confirm verbatim rather than trusting memory.
Mana Drain
