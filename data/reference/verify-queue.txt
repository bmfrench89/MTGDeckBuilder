# DFC RESOLUTION PROBE — research for docs/spec-dfc-enrichment.md, 2026-08-15.
# NOT a card audit. Each line tests one identifier FORM against the same endpoint
# enrichment uses (verify_cards step 2 = POST /cards/collection with {"name": ...}).
# Read the `via ...` suffix in the log: "via scryfall" = EXACT match succeeded,
# "via fuzzy" = exact FAILED and the /cards/named?fuzzy= retry rescued it.
#
# A) FRONT FACE ALONE — the premise of the proposed fix. If these come back
#    "via scryfall", submitting front faces fixes DFC enrichment at round 1.
Murderous Rider
Marang River Regent
Scavenger Regent
Gollum, Silent Slinker
Smaug, the Great Calamity
# B) CONTROL, full combined form — expected "via fuzzy" (this is what fails today).
Murderous Rider // Swift End
Scavenger Regent // Exude Toxin
# C) BACK FACE ALONE — the documented "accepted casualty" case.
Swift End
# D) SINGLE-FACED CONTROL — must be "via scryfall"; proves the probe itself is sound.
Sol Ring
